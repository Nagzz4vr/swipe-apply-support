services:
  - type: web
    name: jobspy-api
    runtime: python
    buildCommand: pip install jobspy==0.31.0 fastapi==0.115.0 uvicorn==0.30.6
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
